<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/toolbar_height"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/mediumBrown">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/toolbar_back_button"
        android:layout_width="36dp"
        android:layout_height="36dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_marginStart="12dp"
        android:padding="3dp"
        app:srcCompat="@drawable/ic_back"
        android:background="@drawable/ripple_background_button_toolbar"
        android:visibility="visible"/>

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/toolbar_meal_categories_button"
        android:layout_width="36dp"
        android:layout_height="36dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/toolbar_meal_countries_button"
        android:layout_marginEnd="6dp"
        android:padding="3dp"
        app:srcCompat="@drawable/ic_meal_category"
        android:background="@drawable/ripple_background_button_toolbar"/>

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/toolbar_meal_countries_button"
        android:layout_width="36dp"
        android:layout_height="36dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="12dp"
        android:padding="3dp"
        app:srcCompat="@drawable/ic_meal_country"
        android:background="@drawable/ripple_background_button_toolbar"/>

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/toolbar_meal_favorite_button"
        android:layout_width="36dp"
        android:layout_height="36dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/toolbar_meal_categories_button"
        android:layout_marginEnd="6dp"
        android:padding="3dp"
        app:srcCompat="@drawable/ic_favorite_disabled"
        android:background="@drawable/ripple_background_button_toolbar"/>

</androidx.constraintlayout.widget.ConstraintLayout>